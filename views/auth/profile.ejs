<!DOCTYPE html>
<html lang="id">

<head>
    <meta charset="UTF-8" />
    <title>
        <%= title %>
    </title>
    <style>
         :root {
            --bg: #f6f8fa;
            --primary: #2563eb;
            --primary-600: #1d4ed8;
            --title: #1e3a8a;
            --text: #0f172a;
            --muted: #64748b;
            --line: #e5e7eb;
            --shadow: 0 8px 30px rgba(0, 0, 0, .06);
            --radius: 16px
        }
        
        * {
            box-sizing: border-box
        }
        
        body {
            font-family: Arial, system-ui;
            background: var(--bg);
            margin: 0;
            padding: 32px
        }
        
        .container {
            max-width: 980px;
            margin: 0 auto
        }
        
        .card {
            background: #fff;
            border-radius: var(--radius);
            box-shadow: var(--shadow);
            overflow: hidden
        }
        
        .card-head {
            display: flex;
            gap: 16px;
            align-items: center;
            padding: 22px 26px;
            background: linear-gradient(180deg, #f8fbff, #fff 60%);
            border-bottom: 1px solid var(--line)
        }
        
        .avatar {
            width: 56px;
            height: 56px;
            border-radius: 999px;
            display: grid;
            place-items: center;
            background: #3f8cff;
            color: #fff;
            font-weight: 800;
            font-size: 22px;
            flex: 0 0 auto;
            box-shadow: 0 2px 8px rgba(0, 0, 0, .12)
        }
        
        .head-meta {
            flex: 1 1 auto;
            min-width: 200px
        }
        
        .title {
            margin: 0;
            color: var(--title);
            font-size: 26px;
            line-height: 1.2
        }
        
        .sub {
            margin: 4px 0 0;
            color: #475569
        }
        
        .pill {
            display: inline-block;
            margin-left: 8px;
            padding: .2rem .55rem;
            border-radius: 999px;
            background: #eef2ff;
            color: #4338ca;
            font-size: 12px;
            border: 1px solid #e0e7ff
        }
        
        .card-body {
            padding: 22px 26px
        }
        
        .grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 18px
        }
        
        .info-block {
            padding: 16px;
            border: 1px solid var(--line);
            border-radius: 12px
        }
        
        .info-row {
            margin: 4px 0
        }
        
        .label {
            color: #475569;
            font-size: 13px
        }
        
        .value {
            color: var(--text);
            font-weight: 600
        }
        
        .divider {
            height: 1px;
            background: #eee;
            margin: 18px 0
        }
        
        .actions {
            display: flex;
            gap: 10px;
            align-items: center;
            flex-wrap: wrap
        }
        
        .btn {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            background: var(--primary);
            color: #fff;
            border: 0;
            border-radius: 10px;
            padding: 10px 14px;
            cursor: pointer;
            transition: .15s ease
        }
        
        .btn:hover {
            background: var(--primary-600);
            transform: translateY(-1px)
        }
        
        .btn-ghost {
            background: #fff;
            color: var(--primary);
            border: 1px solid var(--line)
        }
        
        .btn-small {
            padding: 6px 10px;
            font-size: 13px
        }
        
        .btn-link {
            background: transparent;
            color: var(--primary);
            border: 0;
            padding: 0;
            cursor: pointer
        }
        
        .back {
            color: #2563eb;
            text-decoration: none
        }
        
        .collapse {
            border: 1px solid var(--line);
            border-radius: 12px;
            padding: 16px 16px 12px;
            display: none
        }
        
        .collapse.open {
            display: block
        }
        
        .collapse .legend {
            font-weight: 700;
            margin: 0 0 10px;
            color: var(--text)
        }
        
        label {
            display: block;
            margin: 8px 0 4px;
            color: var(--text);
            font-size: 14px
        }
        
        input {
            width: 100%;
            padding: 10px;
            border: 1px solid var(--line);
            border-radius: 10px
        }
        
        .hlist {
            display: flex;
            flex-direction: column;
            gap: 12px
        }
        
        .hitem {
            display: flex;
            gap: 14px;
            align-items: center;
            justify-content: space-between;
            padding: 12px;
            border: 1px solid var(--line);
            border-radius: 12px;
            background: #fff
        }
        
        .hmeta {
            min-width: 0
        }
        
        .htitle {
            margin: 0;
            font-weight: 700;
            color: #0f172a;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
            max-width: 520px
        }
        
        .hsub {
            color: #64748b;
            font-size: 12px;
            margin-top: 4px
        }
        
        .hact {
            display: flex;
            gap: 8px;
            flex-wrap: wrap
        }
        
        .toast-wrap {
            position: fixed;
            top: 22px;
            left: 50%;
            transform: translateX(-50%);
            z-index: 3;
            display: flex;
            gap: 10px;
            pointer-events: none;
            flex-direction: column
        }
        
        .toast {
            pointer-events: auto;
            display: flex;
            gap: 10px;
            align-items: center;
            justify-content: center;
            min-width: 260px;
            max-width: 520px;
            padding: 10px 14px;
            border-radius: 12px;
            border: 1px solid;
            background: #fff;
            color: #0f172a;
            border-color: #dbeafe;
            box-shadow: var(--shadow);
            animation: toast-in .25s ease, toast-out .25s ease 4.6s forwards
        }
        
        .toast-success {
            border-color: #86efac;
            background: #ecfdf5;
            color: #065f46
        }
        
        .toast-error {
            border-color: #fca5a5;
            background: #fef2f2;
            color: #7f1d1d
        }
        
        .toast .close {
            margin-left: 10px;
            background: transparent;
            border: 0;
            cursor: pointer;
            color: inherit;
            font-size: 18px;
            opacity: .8
        }
        
        .toast .close:hover {
            opacity: 1
        }
        
        @keyframes toast-in {
            from {
                opacity: 0;
                transform: translateY(-6px)
            }
            to {
                opacity: 1;
                transform: translateY(0)
            }
        }
        
        @keyframes toast-out {
            to {
                opacity: 0;
                transform: translateY(-6px)
            }
        }
        
        @media (max-width:840px) {
            .grid {
                grid-template-columns: 1fr
            }
        }
    </style>
</head>

<body>

    <!-- Toast -->
    <div class="toast-wrap" id="toastWrap">
        <% const s = (typeof messages !== 'undefined' && messages.success) ? messages.success : []; %>
            <% const e = (typeof messages !== 'undefined' && messages.error) ? messages.error : []; %>
                <% if (s.length) { %>
                    <div class="toast toast-success">✅ <span><%= s[0] %></span>
                        <button class="close" aria-label="Tutup" onclick="this.parentElement.remove()">×</button>
                    </div>
                    <% } %>
                        <% if (e.length) { %>
                            <div class="toast toast-error">⚠️ <span><%= e[0] %></span>
                                <button class="close" aria-label="Tutup" onclick="this.parentElement.remove()">×</button>
                            </div>
                            <% } %>
    </div>

    <div class="container">
        <div class="card">
            <!-- Header -->
            <div class="card-head">
                <div class="avatar">
                    <%= (user && user.nama ? user.nama[0] : 'U').toUpperCase() %>
                </div>
                <div class="head-meta">
                    <h1 class="title">Profil <span class="pill">Akun</span></h1>
                    <div class="sub">
                        <%= user.email %>
                    </div>
                </div>
                <a href="/" class="btn btn-ghost">← Beranda</a>
            </div>

            <!-- Body -->
            <div class="card-body">
                <div class="grid">
                    <div class="info-block">
                        <div class="info-row">
                            <div class="label">Nama</div>
                            <div class="value">
                                <%= user.nama %>
                            </div>
                        </div>
                        <div class="info-row" style="margin-top:10px">
                            <div class="label">Email</div>
                            <div class="value">
                                <%= user.email %>
                            </div>
                        </div>
                    </div>

                    <div class="info-block">
                        <div class="info-row">
                            <div class="label">Dibuat</div>
                            <div class="value">
                                <%= (user.created_at ? new Date(user.created_at).toLocaleString() : '-') %>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="divider"></div>

                <!-- Aksi -->
                <div class="actions">
                    <button class="btn" id="togglePwd">Ubah Password</button>
                    <a class="back" href="/logres/logout" style="background:#fff;color:#ef4444;border:1px solid var(--line)">Keluar</a>
                </div>

                <!-- Collapsible Password Form -->
                <form id="pwdForm" class="collapse" method="POST" action="/profile/password" autocomplete="off" style="margin-top:12px">
                    <p class="legend">Ganti Password</p>
                    <label for="pwd-old">Password Lama</label>
                    <input id="pwd-old" type="password" name="current_password" autocomplete="current-password" required>
                    <label for="pwd-new" style="margin-top:8px">Password Baru</label>
                    <input id="pwd-new" type="password" name="new_password" minlength="6" autocomplete="new-password" required>
                    <div class="actions" style="margin-top:12px">
                        <button type="submit" class="btn">Simpan</button>
                        <button type="button" class="btn-link" id="cancelPwd">Batal</button>
                    </div>
                </form>

                <div class="divider"></div>

                <!-- RIWAYAT -->
                <h3 style="margin:0 0 10px;color:#0f172a">Riwayat Simpan</h3>

                <% const hist = (typeof items !== 'undefined' && Array.isArray(items)) ? items : []; %>
                    <% if (hist.length === 0) { %>
                        <div class="info-block" style="color:#64748b">Belum ada riwayat.</div>
                        <% } else { %>
                            <div class="hlist">
                                <% hist.forEach(h => { %>
                                    <div class="hitem">
                                        <div class="hmeta">
                                            <h4 class="htitle">
                                                <%= h.title || ('History #' + h.id_history) %>
                                            </h4>
                                            <div class="hsub">
                                                <span><strong><%= h.best_alt %></strong> (skor <%= Number(h.best_score).toFixed(6) %>)</span> ·
                                                <span><%= h.total_alt %> alternatif</span> ·
                                                <span><%= new Date(h.created_at).toLocaleString() %></span>
                                            </div>
                                        </div>
                                        <div class="hact">
                                            <a class="btn btn-small" href="/?history=<%= h.id_history %>">Muat di Beranda</a>
                                        </div>
                                    </div>
                                    <% }) %>
                            </div>
                            <% } %>

            </div>
        </div>
    </div>

    <script>
        // Toggle form password
        const toggleBtn = document.getElementById('togglePwd');
        const formPwd = document.getElementById('pwdForm');
        const cancelBtn = document.getElementById('cancelPwd');
        if (toggleBtn) {
            toggleBtn.addEventListener('click', () => {
                formPwd.classList.toggle('open');
                if (formPwd.classList.contains('open')) {
                    document.getElementById('pwd-old').focus();
                }
            });
        }
        if (cancelBtn) {
            cancelBtn.addEventListener('click', () => formPwd.classList.remove('open'));
        }

        // Auto-dismiss toast + pause on hover
        document.querySelectorAll('.toast').forEach(t => {
            let outTimer = setTimeout(() => t.remove(), 3800);
            t.addEventListener('mouseenter', () => {
                t.style.animationPlayState = 'paused';
                clearTimeout(outTimer);
            });
            t.addEventListener('mouseleave', () => {
                t.style.animationPlayState = 'running';
                outTimer = setTimeout(() => t.remove(), 1200);
            });
        });
    </script>
</body>

</html>